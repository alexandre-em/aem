type LazyImageProps = {
  src: string;
};

type LinkedCardItemProps = {
  title: string;
  description: string;
  date: string;
  url: string;
  src: string; // image url
};

type NavbarComponentsProps = {
  locale: 'en' | 'fr' | 'jp';
};
